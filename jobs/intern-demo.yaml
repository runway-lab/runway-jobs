apiVersion: runway/v1alpha1
kind: Experiment
metadata:
  name: intern-demo
  owner: jurray-jiajun
spec:
  code:
    repo: runway-lab/example-project
    ref: main
  resources:
    gpus: 1
    gpu_type: any
    max_hours: 1
  backends:
    - gcp
  selection:
    policy: eta
    profile_seconds: 120
  run: |
    python train.py --config configs/intern_smoke.yaml
  artifacts:
    uri: gs://${ARTIFACTS_BUCKET}/runs/{run_id}/candidates/{backend_id}/
